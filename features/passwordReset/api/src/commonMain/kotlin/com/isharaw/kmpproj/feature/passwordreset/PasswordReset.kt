package com.isharaw.kmpproj.feature.passwordreset

/**
 * Public password-reset contract (placeholder). The sample screen has no logic yet; this exists so
 * the feature follows the `:api` / `:real` split every other feature uses and is exportable to iOS.
 */
interface PasswordResetGateway
