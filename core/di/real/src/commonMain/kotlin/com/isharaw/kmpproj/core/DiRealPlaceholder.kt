package com.isharaw.kmpproj.core

// Reserved for future di-concern implementations. Currently the di concern is contract-only
// (AppScope lives in :core:di:api); this :real module mirrors the feature api/real convention.
