package com.isharaw.kmpproj.core

// Reserved for future model-concern implementations. Model is pure types + the ExperienceCatalog
// policy (public api in :core:model:api); this :real module mirrors the feature api/real convention.
