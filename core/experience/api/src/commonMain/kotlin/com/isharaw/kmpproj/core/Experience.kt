package com.isharaw.kmpproj.core

/**
 * The **experience** (store brand) a logged-in user gets — part of the [ExperienceSnapshot] the
 * backend resolves at login.
 */
enum class Experience { KEELS, CARGILLS, GLOMARK }
