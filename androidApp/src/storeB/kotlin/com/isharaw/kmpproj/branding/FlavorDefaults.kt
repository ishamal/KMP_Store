package com.isharaw.kmpproj.branding

import com.isharaw.kmpproj.core.Experience

/** CARGILLS (storeB) flavor defaults (see the storeA copy for how flavor source sets select this). */
object FlavorDefaults {
    val defaultExperience = Experience.CARGILLS
}
